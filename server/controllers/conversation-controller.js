const conversation = require("../models/Conversation");
const {createOne,readAll,readOne,updateOne,deleteOne} = require("../services/crud");
const response = require("../services/response");

module.exports = {
  createConversation: (req,res) => {
    try {
             
    } catch (error) {
             response(req,)
    }
  }
}